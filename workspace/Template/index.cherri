#define name Template // TODO: Replace 'Template' with the exact name of your shortcut (must match project folder name)
#define color red
#define glyph circledHeart

@text = "Hello, World!"
show(text)
